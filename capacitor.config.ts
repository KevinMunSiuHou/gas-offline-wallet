import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.uotoslab',
  appName: 'zenwallet---offline-personal-finance',
  webDir: 'dist'
};

export default config;
